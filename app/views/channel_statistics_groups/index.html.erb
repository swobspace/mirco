<% content_for :head do %>
  <meta name="turbolinks-cache-control" content="no-cache">
<% end %>

<h1><%= t('controller.channel_statistics_groups') %></h1>

<div data-controller="datatables">
  <%= content_tag :table, role: :datatable,
        id: "channel_statistics_groups_table",
        class: "table table-bordered table-responsive-xl",
        data: {
          'datatables-target': 'datatable',
          order: [[0, 'asc']].to_json
        } do %>
    <thead>
      <tr>
            <th><%= t('attributes.name') %></th>
            <th class="nosort"><%= t('mirco.action') %></th>
      </tr>
    </thead>
    <tfoot class="search">
      <tr>
            <th></th>
            <th class="nosearch"></th>
      </tr>
    </tfoot>
    <tbody>
      <% @channel_statistics_groups.each do |channel_statistics_group| %>
        <%= content_tag(:tr, id: dom_id(channel_statistics_group), class: dom_class(channel_statistics_group)) do %>
                <td><%= channel_statistics_group.name %></td>
                <td class="text-nowrap">
            <%= show_link channel_statistics_group %>
            <%= edit_link channel_statistics_group %>
            <%= delete_link channel_statistics_group %>
          </td>
        <% end %>
      <% end %>
    </tbody>
  <% end %>
</div>

<br />

<%= back_link %>
<%= new_link ChannelStatisticsGroup %>
