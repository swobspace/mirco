<% content_for :head do %>
  <meta name="turbolinks-cache-control" content="no-cache">
<% end %>

<h1><%= t('controller.alerts') %></h1>

<%= render 'index', alertable: @alertable, alerts: @alerts %>

<br />

<%= back_link %>
