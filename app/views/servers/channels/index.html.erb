<% content_for :head do %>
  <meta name="turbo-cache-control" content="no-cache">
<% end %>

<h1><%= @server %>: <%= t('controller.channels') %></h1>

<%= render partial: 'index', locals: { server: @server, channels: @channels } %>

<br />

<%= back_link %>
