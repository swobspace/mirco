---
:dynamic: true
:enabled: true

:concurrency: 5
:development:
  :concurrency: 1

:queues:
  - default

:schedule:
  channels_fetch_statistics:
    interval: '5m'  # 5min, just as startup value
    class: Channels::FetchStatisticsJob
    description: fetch channel statistics from mirth server
